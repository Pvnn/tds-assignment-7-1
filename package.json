{
  "name": "release-gate",
  "version": "1.0.0",
  "description": "Deterministic CI/CD container release gate policy endpoint",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/release-gate.test.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
